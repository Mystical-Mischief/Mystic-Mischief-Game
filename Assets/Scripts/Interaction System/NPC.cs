using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NPC : Chest
{
    /*public Quest quest;
    public string QuestToFinish;
    public string QuestToActivate;
    private bool questAccepted;
    public bool finishQuest;

    public override bool Interact(Interactor interactor)
    {
        if (quest != null && quest.activeQuest.questName == QuestToActivate && (questAccepted || QuestToActivate == string.Empty))
        {
            quest.UpdateQuest();
        }
        if (quest != null && QuestToActivate != string.Empty && !questAccepted)
        {
            quest.ActivateQuest(QuestToActivate);
            quest.UpdateQuest();
            questAccepted = true;
        }
        return true;
    }*/
}
